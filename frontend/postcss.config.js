// postcss.config.js
// PostCSS processes our CSS. Tailwind and Autoprefixer run as PostCSS plugins.
// Autoprefixer adds vendor prefixes (like -webkit-) automatically for browser compatibility.

export default {
  plugins: {
    tailwindcss: {},
    autoprefixer: {},
  },
}
